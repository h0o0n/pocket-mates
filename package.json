{
  "name": "select-food",
  "version": "2.0.0",
  "private": true,
  "description": "입장 코드로 모여 점심 메뉴를 실시간으로 고르는 웹앱",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web"
  },
  "engines": {
    "node": ">=20"
  }
}
