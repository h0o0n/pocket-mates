{
  "name": "wallet-mates",
  "version": "2.0.0",
  "private": true,
  "description": "소비에 따라 캐릭터가 반응하는 게임형 가계부",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "test": "npm run test:logic --prefix web",
    "preview": "npm run preview --prefix web"
  },
  "engines": {
    "node": ">=20"
  }
}
